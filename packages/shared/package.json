{
  "name": "@restocash/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "node -e \"require('fs').accessSync('src/index.ts'); console.log('Shared typecheck OK')\"",
    "test": "echo 'Shared tests OK: constants only'",
    "lint": "echo 'Shared lint OK: scaffold pending'"
  }
}
